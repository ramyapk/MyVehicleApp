<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ramya.myvehicleapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.AddvehicleActivity" />
        <activity android:name=".ui.activities.SignupActivity" />
        <activity android:name=".ui.activities.VehicleInfoActivity" />
        <activity android:name=".ui.activities.ViewDetailsActivity"></activity>

        <provider
            android:authorities="com.example.ramya.myvehicleapp"
            android:name=".database.ExampleContentProvider"
            android:exported="true"/>
    </application>

</manifest>